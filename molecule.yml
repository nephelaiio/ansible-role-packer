---
driver:
  name: docker

docker:
  containers:
      - name: ansible-packer-jessie
        image: debian
        image_version: jessie
        ansible_groups:
          - test
      - name: ansible-packer-xenial
        image: ubuntu
        image_version: xenial
        ansible_groups:
          - test
      - name: ansible-packer-centos7
        image: centos
        image_version: 7
        ansible_groups:
          - test
      - name: ansible-packer-arch
        image: pritunl/archlinux
        image_version: latest
        dockerfile: docker/arch.dockerfile
        command: /bin/bash
        ansible_groups:
          - test

ansible:
  requirements_file: dependencies.yml

verifier:
  name: testinfra
